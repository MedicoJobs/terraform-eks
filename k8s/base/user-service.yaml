apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: medicojobs
  labels: &userLabels
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: medicojobs-platform
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels: *userLabels
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels: *userLabels
    spec:
      containers:
        - name: user-service
          image: 123456789012.dkr.ecr.ap-south-1.amazonaws.com/medicojob-user-service:latest
          ports:
            - containerPort: 5001
          envFrom:
            - configMapRef:
                name: medicojobs-config
            - secretRef:
                name: medicojobs-secrets
                optional: true
          readinessProbe:
            tcpSocket:
              port: 5001
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 5001
            initialDelaySeconds: 45
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 750m
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: medicojobs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: medicojobs-platform
  ports:
    - name: http
      port: 5001
      targetPort: 5001
